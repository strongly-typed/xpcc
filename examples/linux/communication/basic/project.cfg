[communication]
container = receiver
source = ../../../communication/xml/communication.xml

[environment]
LINKCOM* = -lpthread -lzmqpp -lzmq -lpcap
LDFLAGS* = -L/usr/local/opt/libpcap/lib

[build]
device = hosted
buildpath = ${xpccpath}/build/linux/${name}
